import express from 'express';
import * as problemController from '../controllers/problemController';

const router = express.Router();

router.get('/', );
router.get('/:id', );

// Make sure that the user is authenticated and is Problem-setter
router.post('/', );
router.put('/:id', );
router.delete('/:id', );

export default router;
